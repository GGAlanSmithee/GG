import Interface from './../../utility/interface';

export default class ISceneService extends Interface {
    constructor(derrivedClass) {
        super(ISceneService, derrivedClass);
    }
    
    createScene(mesh) {
    }
}