import Interface from './../../utility/interface';

export default class ILevelService extends Interface {
    constructor(derrivedClass) {
        super(ILevelService, derrivedClass);
    }
    
    loadLevel(path) {
    }
}