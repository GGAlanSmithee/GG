import Interface from './../../utility/interface';

export default class IMeshLoader extends Interface {
    constructor(derrivedClass) {
        super(IMeshLoader, derrivedClass);
    }
    
    load(path) {
    }
}